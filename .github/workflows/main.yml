name: CI Java

on:
  workflow_call

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name : Checkout🚀 
      uses: actions/checkout@v3.5.3
      
    - name: ListFiles🧨
      run: ls -ltr
